# Workflow Link

This folder records how the private project uses the public workflow.

Recommended options:

- Keep this file as a pointer to the public workflow repository.
- Or replace `workflow/` with a Git submodule that points to the public workflow repository.

Public workflow path:

```text
{{WORKFLOW_REPO_PATH}}
```
